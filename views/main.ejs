<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>IoT管理</title>
		<link rel="stylesheet" href="/css/style.css">
	</head>
	<body>
		<header>
			<a href="/" class="header_logo">IoT管理画面</a>
		</header>

		<% var names = ["Office", "Home", "Unknown"]; %>
		
		<div class="container">
			<ul class="state-wrapper">
				<% items.forEach((item) => { %>
					<% 
						var nextstate = item.state+1;
						if(nextstate>2) {
							nextstate = 1;
						}
					%>
					<li>
						<span><%= item.user %></span>
						<form action="/change/<%= nextstate %>" method="post" class="inline-form">
							<input type="submit" value="<%= names[item.state-1] %>" /><br />
						</form>
					</li>
				<% }) %>
			</ul>
		</div>
	</body>
</html>