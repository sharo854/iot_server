<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>IoT管理</title>
		<link rel="stylesheet" href="/css/style.css">
	</head>
	<body>
		<header>
			<a href="/" class="header-logo">IoT管理画面</a>
		</header>

		<% var names = ["Office", "Home", "Unknown"]; %>
		
		<div class="state-wrapper">
			<div class="container">
				<ul>
					<% items.forEach((item) => { %>
						<% 
							var nextstate = item.state+1;
							if(nextstate>2) {
								nextstate = 1;
							}
						%>
						<li>
							<span><%= item.user %></span>
							<form action="/change/<%= nextstate %>" method="post">
								<input type="submit" value="<%= names[item.state-1] %>" class="button" />
							</form>
						</li>
					<% }) %>
				</ul>
			</div>
		</div>
	</body>
</html>